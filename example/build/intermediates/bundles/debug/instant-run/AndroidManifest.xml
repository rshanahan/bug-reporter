<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.stkent.bugshaker"
    android:versionCode="4"
    android:versionName="1.3.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="24" />

    <!-- Include required permissions for Google Maps API to run. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <application
        name="com.example.bugshaker.CustomApplication"
        android:name="com.android.tools.fd.runtime.BootstrapApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.bugshaker.UnsecuredActivity"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.bugshaker.SecuredActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.example.bugshaker.MapViewActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.example.bugshaker.MapFragmentActivity"
            android:launchMode="singleTop" />
        <activity
            android:name="com.example.bugshaker.SurfaceViewActivity"
            android:launchMode="singleTop" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCT0Me0Yy9EyZRYye77e0y7QjtlBMENyTo" />

        <activity
            android:name="com.google.android.gms.common.api.GoogleApiActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="com.github.stkent.bugshaker.firebaseinitprovider"
            android:exported="false"
            android:initOrder="100" />
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.github.stkent.bugshaker.bugshaker.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true" >
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider> <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.SEND_MULTIPLE" /> -->
        <!-- <data android:scheme="mailto" /> -->
        <!-- <data android:mimeType="text/plain" /> -->
        <!-- <data android:mimeType="image/*" /> -->
        <!-- <data android:mimeType="video/*" /> -->
        <!-- </intent-filter> -->
        <activity android:name="com.github.stkent.bugshaker.MainActivity" />
    </application>

</manifest>